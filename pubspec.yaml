name: get_it_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  cupertino_icons: ^1.0.0
  kiwi: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  kiwi_generator: ^2.1.1

flutter:
  uses-material-design: true
